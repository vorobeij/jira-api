/**
 * ⚠️ Do not edit manually ⚠️
 * Generated by openapi-generate. See project's `openapi-generate.sh` for details
 */
package jira.api.apis

import jira.api.models.EntityProperty
import jira.api.models.PropertyKeys
import retrofit2.http.Body
import retrofit2.http.GET

interface AppPropertiesApi {

    /**
     * GET /rest/atlassian-connect/1/addons/{addonKey}/properties
     *
     * Gets all the properties of an app.

     **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
    Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
     * @param addonKey The key of the app, as defined in its descriptor.  (example: null)
     *
     * @return PropertyKeys
     */
    @GET("/rest/atlassian-connect/1/addons/{addonKey}/properties")
    suspend fun addonPropertiesResourceGetAddonPropertiesGet(
        @Body addonKey: String
    ): PropertyKeys

    /**
     * GET /rest/atlassian-connect/1/addons/{addonKey}/properties/{propertyKey}
     *
     * Returns the key and value of an app's property.

     **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
    Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
     * @param addonKey The key of the app, as defined in its descriptor.  (example: null)
     * @param propertyKey The key of the property.  (example: null)
     *
     * @return EntityProperty
     */
    @GET("/rest/atlassian-connect/1/addons/{addonKey}/properties/{propertyKey}")
    suspend fun addonPropertiesResourceGetAddonPropertyGet(
        @Body addonKey: String,
        @Body propertyKey: String
    ): EntityProperty
}