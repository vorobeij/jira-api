/**
 * ⚠️ Do not edit manually ⚠️
 * Generated by openapi-generate. See project's `openapi-generate.sh` for details
 */
package jira.api.models

import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable

/**
 * The approval configuration of a status within a workflow. Applies only to Jira Service Management approvals.
 *
 * @param active, required, Whether the approval configuration is active.
 * @param conditionType, required, How the required approval count is calculated. It may be configured to require a specific number of approvals, or approval by a percentage of approvers. If the approvers source field is Approver groups, you can configure how many approvals per group are required for the request to be approved. The number will be the same across all groups.
 * @param conditionValue, required, The number or percentage of approvals required for a request to be approved. If `conditionType` is `number`, the value must be 20 or less. If `conditionType` is `percent`, the value must be 100 or less.
 * @param exclude, optional, A list of roles that should be excluded as possible approvers.
 * @param fieldId, required, The custom field ID of the "Approvers" or "Approver Groups" field.
 * @param prePopulatedFieldId, optional, The custom field ID of the field used to pre-populate the Approver field. Only supports the "Affected Services" field.
 * @param transitionApproved, required, The numeric ID of the transition to be executed if the request is approved.
 * @param transitionRejected, required, The numeric ID of the transition to be executed if the request is declined.
 */
@Serializable
data class ApprovalConfiguration(
    @SerialName("active") val active: String,
    @SerialName("conditionType") val conditionType: String,
    @SerialName("conditionValue") val conditionValue: String,
    @SerialName("exclude") val exclude: List<String>? = null,
    @SerialName("fieldId") val fieldId: String,
    @SerialName("prePopulatedFieldId") val prePopulatedFieldId: String? = null,
    @SerialName("transitionApproved") val transitionApproved: String,
    @SerialName("transitionRejected") val transitionRejected: String
)