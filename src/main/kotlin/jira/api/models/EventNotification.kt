/**
 * ⚠️ Do not edit manually ⚠️
 * Generated by openapi-generate. See project's `openapi-generate.sh` for details
 */
package jira.api.models

import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable

/**
 * Details about a notification associated with an event.
 *
 * @param emailAddress, optional, The email address.
 * @param expand, optional, Expand options that include additional event notification details in the response.
 * @param field, optional, The custom user or group field.
 * @param group, optional, The specified group.
 * @param id, optional, The ID of the notification.
 * @param notificationType, optional, Identifies the recipients of the notification.
 * @param parameter, optional, As a group's name can change, use of `recipient` is recommended. The identifier associated with the `notificationType` value that defines the receiver of the notification, where the receiver isn't implied by `notificationType` value. So, when `notificationType` is:

 *  `User` The `parameter` is the user account ID.
 *  `Group` The `parameter` is the group name.
 *  `ProjectRole` The `parameter` is the project role ID.
 *  `UserCustomField` The `parameter` is the ID of the custom field.
 *  `GroupCustomField` The `parameter` is the ID of the custom field.
 * @param projectRole, optional, The specified project role.
 * @param recipient, optional, The identifier associated with the `notificationType` value that defines the receiver of the notification, where the receiver isn't implied by the `notificationType` value. So, when `notificationType` is:

 *  `User`, `recipient` is the user account ID.
 *  `Group`, `recipient` is the group ID.
 *  `ProjectRole`, `recipient` is the project role ID.
 *  `UserCustomField`, `recipient` is the ID of the custom field.
 *  `GroupCustomField`, `recipient` is the ID of the custom field.
 * @param user, optional, The specified user.
 */
@Serializable
data class EventNotification(
    @SerialName("emailAddress") val emailAddress: String? = null,
    @SerialName("expand") val expand: String? = null,
    @SerialName("field") val field: FieldDetails? = null,
    @SerialName("group") val group: GroupName? = null,
    @SerialName("id") val id: Long? = null,
    @SerialName("notificationType") val notificationType: String? = null,
    @SerialName("parameter") val parameter: String? = null,
    @SerialName("projectRole") val projectRole: ProjectRole? = null,
    @SerialName("recipient") val recipient: String? = null,
    @SerialName("user") val user: UserDetails? = null
)