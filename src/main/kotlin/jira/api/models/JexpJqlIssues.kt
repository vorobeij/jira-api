/**
 * ⚠️ Do not edit manually ⚠️
 * Generated by openapi-generate. See project's `openapi-generate.sh` for details
 */
package jira.api.models

import kotlinx.serialization.*

/**
 * The JQL specifying the issues available in the evaluated Jira expression under the `issues` context variable. Not all issues returned by the JQL query are loaded, only those described by the `startAt` and `maxResults` properties. To determine whether it is necessary to iterate to ensure all the issues returned by the JQL query are evaluated, inspect `meta.issues.jql.count` in the response.
 *
 * @param maxResults, optional, The maximum number of issues to return from the JQL query. Inspect `meta.issues.jql.maxResults` in the response to ensure the maximum value has not been exceeded.
 * @param query, optional, The JQL query.
 * @param startAt, optional, The index of the first issue to return from the JQL query.
 * @param validation, optional, Determines how to validate the JQL query and treat the validation results.
 */
@Serializable
data class JexpJqlIssues(
    @SerialName("maxResults") val maxResults: Int? = null,
    @SerialName("query") val query: String? = null,
    @SerialName("startAt") val startAt: Long? = null,
    @SerialName("validation") val validation: String = strict
)